import * as React from 'react';
import { Star } from '../../../src';

export const App = () => {
    return <Star innerRadius={0.5} pointCount={5} style={{ width: 100, height: 100, backgroundColor: '#0000ff' }} />;
};
